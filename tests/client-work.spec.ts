import { test, expect } from '@playwright/test';

test.describe('EPAM - Client Work navigation', () => {
  test('navigates to Client Work and verifies text', async ({ page }) => {
    /* TEST_BODY_PLACEHOLDER */
  });
});
